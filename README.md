# SwiftCart E-Commerce

স্বাগতম **SwiftCart** প্রজেক্টে! এটি একটি সম্পূর্ণ রেসপনসিভ ই-কমার্স ওয়েবসাইট যা **HTML, Tailwind CSS, DaisyUI এবং Vanilla JavaScript** ব্যবহার করে তৈরি করা হয়েছে। এই প্রজেক্টে FakeStoreAPI ব্যবহার করে রিয়েল-টাইম প্রোডাক্ট ডেটা এবং ক্যাটাগরি দেখানো হয়েছে।

## 🔗 প্রজেক্ট লিংকসমূহ

- **লাইভ ওয়েবসাইট:** [https://glittery-sprinkles-db91ed.netlify.app/](https://glittery-sprinkles-db91ed.netlify.app/)
- **গিটহাব রিপোজিটরি:** [https://github.com/tarikulhazra/SwiftCart](https://github.com/tarikulhazra/SwiftCart)

---

## 🚀 ফিচারসমূহ

- **ডায়নামিক প্রোডাক্ট লোডিং:** API থেকে সরাসরি প্রোডাক্ট এবং ক্যাটাগরি লোড হয়।
- **ক্যাটাগরি ফিল্টার:** ব্যবহারকারীরা ক্যাটাগরি অনুযায়ী প্রোডাক্ট ফিল্টার করতে পারেন।
- **প্রোডাক্ট ডিটেইলস:** "Details" বাটনে ক্লিক করলে মডালে প্রোডাক্টের বিস্তারিত তথ্য দেখা যায়।
- **কার্ট সিস্টেম:** কার্টে প্রোডাক্ট যুক্ত করার সুবিধা এবং ন্যাভবারে কার্ট কাউন্ট আপডেট।
- **রেসপনসিভ ডিজাইন:** মোবাইল, ট্যাবলেট এবং ডেক্সটপ—সব ডিভাইসে সুন্দরভাবে কাজ করে।

---

## 📝 প্রশ্নের উত্তর (অ্যাসাইনমেন্ট রিকোয়ারমেন্ট)

### ১) null এবং undefined এর মধ্যে পার্থক্য কী?
**উত্তর:**
জাভাস্ক্রিপ্টে `undefined` মানে হলো কোনো ভেরিয়েবল ডিক্লেয়ার করা হয়েছে কিন্তু তাতে কোনো মান (value) এসাইন বা সেট করা হয়নি। এটি সাধারণত ডিফল্ট অবস্থা। অন্যদিকে, `null` হলো একটি এসাইন করা ভ্যালু, যা দিয়ে প্রোগ্রামার ইচ্ছাকৃতভাবে বোঝান যে ওই ভেরিয়েবলটি বর্তমানে খালি বা তার কোনো অস্তিত্ব নেই। সংক্ষেপে: `undefined` অটোমেটিক আসে, আর `null` ম্যানুয়ালি সেট করা হয়।

### ২) map() ফাংশন এর কাজ কী এবং এটি forEach() থেকে কীভাবে আলাদা?
**উত্তর:**
`map()` ফাংশন একটি অ্যারের প্রতিটি উপাদানের ওপর লুপ চালায় এবং একটি নির্দিষ্ট অপারেশন বা ফাংশন অ্যাপ্লাই করে **নতুন একটি অ্যারে রিটার্ন করে**। এটি মূল অ্যারে পরিবর্তন করে না।
**পার্থক্য:** প্রধান পার্থক্য হলো `map()` কাজ শেষে একটি নতুন অ্যারে রিটার্ন করে, কিন্তু `forEach()` কিছু রিটার্ন করে না (undefined রিটার্ন করে)। `forEach()` শুধুমাত্র অ্যারের ওপর ইটারেট বা লুপ চালানোর জন্য ব্যবহৃত হয়, ডেটা মডিফাই করে নতুন অ্যারে বানাতে নয়।

### ৩) == এবং === এর মধ্যে পার্থক্য কী?
**উত্তর:**
`==` (Loose Equality) শুধুমাত্র ভ্যালু চেক করে, কিন্তু ডেটা টাইপ চেক করে না। যেমন: `5 == "5"` সত্য (True) হবে কারণ ভ্যালু একই।
`===` (Strict Equality) ভ্যালু এবং ডেটা টাইপ—দুটোই চেক করে। যেমন: `5 === "5"` মিথ্যা (False) হবে, কারণ একটি নাম্বার টাইপ এবং অন্যটি স্ট্রিং টাইপ।

### ৪) API ডেটা ফেচিং-এ async/await এর গুরুত্ব কী?
**উত্তর:**
API থেকে ডেটা ফেচ করা একটি সময়সাপেক্ষ কাজ (asynchronous)। `async/await` ব্যবহার করলে কোডটি দেখতে synchronous বা সাধারণ লাইনের মতো মনে হয়, ফলে কোড পড়া এবং বোঝা সহজ হয়। `await` কিওয়ার্ডটি প্রমিজ (Promise) সলভ বা রিজেক্ট হওয়া পর্যন্ত কোড এক্সিকিউশন থামিয়ে রাখে, ফলে `.then()` চেইন ব্যবহার করার জটিলতা কমে যায় এবং `try-catch` ব্লক দিয়ে সহজেই এরর হ্যান্ডেল করা যায়।

### ৫) জাভাস্ক্রিপ্টে Scope (Global, Function, Block) এর ধারণা ব্যাখ্যা করো।
**উত্তর:**
জাভাস্ক্রিপ্টে স্কোপ ৩ ধরনের হতে পারে:
১. **Global Scope:** যে ভেরিয়েবল কোনো ফাংশন বা ব্লকের বাইরে ডিক্লেয়ার করা হয়, তা পুরো ফাইলে যেকোনো জায়গা থেকে এক্সেস করা যায়।
২. **Function Scope:** `var` দিয়ে ফাংশনের ভেতরে ডিক্লেয়ার করা ভেরিয়েবল শুধু ওই নির্দিষ্ট ফাংশনের ভেতরেই কাজ করে, বাইরে থেকে পাওয়া যায় না।
৩. **Block Scope:** `let` এবং `const` দিয়ে ডিক্লেয়ার করা ভেরিয়েবল কোনো ব্লক বা `{ }` (যেমন if, for loop) এর ভেতরে থাকলে, সেটি শুধু ওই ব্লকের ভেতরেই কাজ করে।

---
**Developed by:** Tarikul Hazra